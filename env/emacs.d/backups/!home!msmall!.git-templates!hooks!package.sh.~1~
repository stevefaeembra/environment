#!/usr/bin/env bash


# Check Build works.
ERROR='Build failure';
echo "Building Application e.g. 'mvn clean package'.\n"
result=`mvn clean package | grep "$error" | awk '{ print $2 " " $3 }'`;

if [ "$result" ]; then
  exit 1;
fi

exit 0
